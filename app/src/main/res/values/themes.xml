<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
     <style name="Theme.Kaapav" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/kaapav_gold</item>
        <item name="colorOnPrimary">@color/kaapav_bg</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/kaapav_bg</item>
        <item name="android:statusBarColor">@color/kaapav_bg</item>
        <item name="android:navigationBarColor">@color/kaapav_bg</item>

        <!-- Text -->
        <item name="android:textColorPrimary">@color/kaapav_text</item>
        <item name="android:textColorSecondary">@color/kaapav_sub</item>

        <!-- Material components tweaks -->
        <item name="materialCardViewStyle">@style/Widget.MaterialComponents.CardView</item>
    </style>
</resources>

